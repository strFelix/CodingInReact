import React from "react";

const NovoConteudo = (props) =>
{
    return(
        <div>
            <h2>Adicionando novo componente!</h2>
            <p>{props.value}</p>
        </div>
    );
}

export default NovoConteudo;